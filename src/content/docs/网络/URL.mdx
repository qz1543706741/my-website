---
title: 浏览器地址
---

## URL

`URL`是一个固定格式的字符串

`schema://domain:port/path?query#hash`

它表达了：
从网络中**哪台计算机（domain）中的哪个程序（port）**寻找**哪个服务（path）**，并注明了获取服务的**具体细节（path）**，以及要用什么样的**协议通信（schema）**
这里面包含了一些细节：

- 当`schema`是http端口为80，https端口为443
- `schema`、`domain`、`path`是必填的，其他的根据具体情况填写
